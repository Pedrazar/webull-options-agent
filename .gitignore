# secrets
.env

# logs (Webull SDK logs request/response bodies incl. app key + account id)
*.log
*.log.*

# deps / build
node_modules/
dist/

# wheel-trades.jsonl, wheel-state.json, and daily-reviews/ are
# INTENTIONALLY tracked, not ignored — as of the 2026-09-16 move to GitHub
# Actions, they're the durable, git-committed persistence layer across
# ephemeral Actions job runs (see CLAUDE.md's GitHub Actions section). Only
# genuinely local secrets/logs stay ignored above.
